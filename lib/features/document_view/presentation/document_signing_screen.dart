import 'package:flutter/material.dart';

class DocumentSigningScreen extends StatelessWidget {
  const DocumentSigningScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DocumentSigningScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
