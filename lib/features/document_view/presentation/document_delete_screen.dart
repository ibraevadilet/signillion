import 'package:flutter/material.dart';

class DocumentDeleteScreen extends StatelessWidget {
  const DocumentDeleteScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DocumentDeleteScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
