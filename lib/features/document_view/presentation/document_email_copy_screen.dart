import 'package:flutter/material.dart';

class DocumentEmailCopyScreen extends StatelessWidget {
  const DocumentEmailCopyScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DocumentEmailCopyScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
