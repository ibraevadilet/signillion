import 'package:flutter/material.dart';

class DocumentMoveToScreen extends StatelessWidget {
  const DocumentMoveToScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DocumentMoveToScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
