import 'package:flutter/material.dart';

class DocumentDuplicateScreen extends StatelessWidget {
  const DocumentDuplicateScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DocumentDuplicateScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
