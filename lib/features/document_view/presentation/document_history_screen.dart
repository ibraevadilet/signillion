import 'package:flutter/material.dart';

class DocumentHistoryScreen extends StatelessWidget {
  const DocumentHistoryScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DocumentHistoryScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
