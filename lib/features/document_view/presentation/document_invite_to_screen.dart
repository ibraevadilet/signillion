import 'package:flutter/material.dart';

class DocumentInviteToScreen extends StatelessWidget {
  const DocumentInviteToScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DocumentInviteToScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
